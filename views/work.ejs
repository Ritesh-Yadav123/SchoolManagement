<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Work Panel</title>
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      background: linear-gradient(135deg, #1d2b64, #f8cdda);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    h1 {
      color: #fff;
      font-size: 24px;
      margin-bottom: 40px;
      text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.4);
    }

    .dropdown-container {
      position: relative;
    }

    #work {
      padding: 14px 20px;
      font-size: 16px;
      border-radius: 14px;
      border: none;
      background: rgba(255, 255, 255, 0.95);
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.25);
      min-width: 280px;
      appearance: none;
      cursor: pointer;
      transition: 0.3s ease-in-out;
    }

    #work:hover {
      transform: scale(1.05);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
    }

    #work:focus {
      outline: none;
      box-shadow: 0 0 0 3px rgba(240, 150, 255, 0.6);
    }

    .dropdown-container::after {
      content: "▼";
      position: absolute;
      top: 50%;
      right: 16px;
      transform: translateY(-50%);
      color: #555;
      pointer-events: none;
      font-size: 14px;
    }

    option {
      padding: 10px;
    }

    #highlight {
      color: green;
      font-size: 34px;
      font-weight: bolder;
      margin-top: 0;
      position: absolute;
      top: 18px;
      text-shadow: 2px 2px black;
      transition: 2s;
    }

   @media screen and (max-width: 480px) {
  body {
    justify-content: flex-start; /* Shift content to top */
    padding-top: 100px; /* Add spacing */
  }

  #highlight {
    position: relative;
    top: 0;
    text-align: center;
    font-size: 24px;
    margin: 10px 20px;
  }

  h1 {
    font-size: 18px;
    text-align: center;
    padding: 10px;
  }

  .dropdown-container {
    width: 90%;
    margin: 20px auto;
  }

  #work {
    width: 100%;
  }
}

  </style>
</head>
<body>
<div id="highlight">
  Welcome To School Management System
  <h6 id="dev" style="color: red; text-align: center;">developed by RITTUtech</h6>
</div>

  <h1>Class: <%= studentData.class %> | Section: <%= studentData.section %></h1>

  <div class="dropdown-container">
    <select name="work" id="work">
      <option>Admission</option>
      <option>Notice</option>
      <option>Students</option>
      <option>Fee Management</option>
      <option>Marksheet</option>
      <option>ClassRoutine</option>
    </select>
  </div>
<script>
  const element=document.getElementById('highlight');
  element.style.opacity=0;
  setInterval(()=>{
    element.style.opacity=1;
    document.getElementById('dev').style.opacity=0;
    setInterval(()=>{document.getElementById('dev').style.opacity=1},2000);
  },2000);

</script>
</body>
</html>
